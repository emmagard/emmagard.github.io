import React from "react";

const PageTitle = ({title}) => {
  return (
    <h1 className="font-display text-4xl sm:text-5xl font-bold mb-10 text-white">{title}</h1>
  );
};

export default PageTitle;